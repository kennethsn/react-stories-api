import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  a: {
    color: 'inherit',
  },
  iframe: {
    height: '100vh',
    width: '100%',
  },
}));

export default useStyles;
