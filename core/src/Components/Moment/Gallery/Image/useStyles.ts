import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  dialog: {
    background: 'none',
    boxShadow: 'none',
    maxWidth: '60vw',
  },
}));

export default useStyles;
