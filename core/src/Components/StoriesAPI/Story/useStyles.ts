import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  box: {
    display: 'flex',
    height: '100vh',
    maxHeight: '100vh',
    textAlign: 'center',
    width: '100%',
  },
}));

export default useStyles;
