import type { StoryLayoutProps } from './StoryLayout.types';

// KSN TODO: Make swiper pagination sticky
// KSN TODO: Add bottom padding to the story layout
// KSN TODO: Mobile navigator
export default function StoryLayoutMobile({ children }: StoryLayoutProps) {
  return children;
}
