export type BookProps = {
  accentColor?: string;
  author?: string;
  coverColor?: string;
  description?: string;
  onClick?: () => void;
  style?: React.CSSProperties;
  subtitle?: string;
  textColor?: string;
  title: string;
};
